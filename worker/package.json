{
  "name": "web3-university-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --ip 127.0.0.1 --port 8787 --inspector-port 9239 --persist-to ../.wrangler-state --show-interactive-dev-session false",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply DB --local --persist-to ../.wrangler-state",
    "migrate:remote": "wrangler d1 migrations apply DB",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.6.3",
    "viem": "^2.21.19"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.86.1"
  }
}
